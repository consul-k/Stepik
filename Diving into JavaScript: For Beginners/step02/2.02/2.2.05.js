/*

Создайте две переменные source и destination. Присвойте переменной source значение "sigma". 
Затем скопируйте значение переменной source в переменную destination. Измените значение source на "delta". 
Выведите значениe source, a затем значение destination с новой строки.

Sample Input:


Sample Output:

delta
sigma

*/

let source;
let destination;
source = 'sigma';
destination = source;
source = 'delta';
console.log(source);
console.log(destination);
